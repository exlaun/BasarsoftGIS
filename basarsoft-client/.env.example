# Copy to .env.local and adjust per environment. Without it the app targets the local dev API.
# The API's CORS policy must list this app's own origin (Cors:AllowedOrigins in appsettings.json).
VITE_API_BASE_URL=http://localhost:5032
